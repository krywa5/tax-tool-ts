import React, { FunctionComponent } from "react";
import { AppTemplate } from "templates/app-template/AppTemplate";

export const TaxTool: FunctionComponent = () => {
  return <AppTemplate>TaxTool Page</AppTemplate>;
};
