import { AppTemplate } from "templates/app-template/AppTemplate";
import React, { FunctionComponent } from "react";

export const Login: FunctionComponent = () => {
  return <AppTemplate>LoginPage</AppTemplate>;
};
